# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-si446x
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX

list(APPEND si446x_sources
)

set(si446x_sources "${si446x_sources}" PARENT_SCOPE)
if(NOT si446x_sources)
    MESSAGE(STATUS "No C++ sources... skipping lib/")
    return()
endif(NOT si446x_sources)

add_library(gnuradio-si446x SHARED ${si446x_sources})
target_link_libraries(gnuradio-si446x gnuradio::gnuradio-runtime)
target_include_directories(gnuradio-si446x
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<INSTALL_INTERFACE:include>
  )
set_target_properties(gnuradio-si446x PROPERTIES DEFINE_SYMBOL "gnuradio_si446x_EXPORTS")

if(APPLE)
    set_target_properties(gnuradio-si446x PROPERTIES
        INSTALL_NAME_DIR "${CMAKE_INSTALL_PREFIX}/lib"
    )
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
GR_LIBRARY_FOO(gnuradio-si446x)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories()
# List all files that contain Boost.UTF unit tests here
list(APPEND test_si446x_sources
)
# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-si446x)

if(NOT test_si446x_sources)
    MESSAGE(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_si446x_sources)

foreach(qa_file ${test_si446x_sources})
    GR_ADD_CPP_TEST("si446x_${qa_file}"
        ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file}
    )
endforeach(qa_file)
